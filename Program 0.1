if(!zdi[(stav.d+1)%4])
{
  stav.d=(stav.d+1)%4;
  return stav.d;
}
if(!zdi[(stav.d)%4])
{
  stav.d=(stav.d)%4;
  return stav.d;
}
if(!zdi[(stav.d+3)%4])
{
  stav.d=(stav.d+3)%4;
  return stav.d;
}
if(!zdi[(stav.d+2)%4])
{
  stav.d=(stav.d+2)%4;
  return stav.d;
}
